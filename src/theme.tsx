// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(13, 27, 232)', // Set the primary color to rgb(13, 27, 232)
    },
  },
});

export default theme;